/** @type {import('next').NextConfig} */
const nextConfig = {
    hrefs: {
        domains: ['https://ecommerce-site-five-rho.vercel.app/'],
        domains: ['https://simple-dashboard-ui-one.vercel.app/']
    },
}

export default nextConfig